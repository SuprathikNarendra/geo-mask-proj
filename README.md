# geo-mask-proj
This project explores how to protect sensitive GPS data while still allowing location‑based services to function. Instead of sharing exact coordinates, the system applies differential privacy techniques (Laplace noise) to “blur” user locations within a configurable radius. 
